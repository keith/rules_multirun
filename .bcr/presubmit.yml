matrix:
  platform: ["macos", "ubuntu2204", "windows"]
  bazel: ["6.x", "7.x", "8.x", "9.x", "rolling"]

tasks:
  verify_targets:
    name: "Verify build targets"
    platform: ${{ platform }}
    bazel: ${{ bazel }}
    test_targets:
      - '@rules_multirun//tests/...'
