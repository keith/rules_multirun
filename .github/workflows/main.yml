name: CI
"on":
  push:
    branches: [main]
  pull_request:
    branches: [main]
  schedule:
    # Every day at 11:03 UTC.
    - cron: '3 11 * * *'

jobs:
  windows_test:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: bazelisk test //tests/...
